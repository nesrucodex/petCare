import React from "react";

import { SatelliteDish } from "lucide-react";

type Props = {};

const Logo = (props: Props) => {
  return <SatelliteDish size={35} className="text-white" />;
};

export default Logo;
